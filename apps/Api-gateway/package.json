{
  "name": "@lazypeps/Api-gateway",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@lazypeps/Api-gateway:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@lazypeps/Api-gateway:build:development"
          },
          "production": {
            "buildTarget": "@lazypeps/Api-gateway:build:production"
          }
        }
      }
    }
  }
}
